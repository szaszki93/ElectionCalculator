<UserControl
    x:Class="ElectionCalculatorView.View.ResultView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:l="clr-namespace:ElectionCalculatorView.Resource"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="700"
    d:DesignWidth="1200"
    mc:Ignorable="d">

    <StackPanel VerticalAlignment="Center">
        <Grid HorizontalAlignment="Center">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <Label HorizontalAlignment="Center" Content="{x:Static l:Language.CandidatesResults}" />
                <ListView
                    HorizontalAlignment="Stretch"
                    IsSynchronizedWithCurrentItem="True"
                    ItemsSource="{Binding Data.Results}">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn DisplayMemberBinding="{Binding Path=NumberOfVotes}" Header="{x:Static l:Language.NumberOfVotes}" />
                            <GridViewColumn DisplayMemberBinding="{Binding Path=Candidate.Name}" Header="{x:Static l:Language.Name}" />
                            <GridViewColumn DisplayMemberBinding="{Binding Path=Candidate.Party}" Header="{x:Static l:Language.Party}" />
                        </GridView>
                    </ListView.View>
                </ListView>
            </StackPanel>

            <StackPanel Grid.Column="1">
                <Label HorizontalAlignment="Center" Content="{x:Static l:Language.PartiesResults}" />
                <ListView
                    HorizontalAlignment="Center"
                    IsSynchronizedWithCurrentItem="True"
                    ItemsSource="{Binding Data.PartiesResults}">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn DisplayMemberBinding="{Binding Path=NumberOfVotes}" Header="{x:Static l:Language.NumberOfVotes}" />
                            <GridViewColumn DisplayMemberBinding="{Binding Path=Party}" Header="{x:Static l:Language.Party}" />
                        </GridView>
                    </ListView.View>
                </ListView>

                <StackPanel HorizontalAlignment="Center" Orientation="Horizontal">
                    <TextBlock Height="23" Text="{x:Static l:Language.NumberOfValidVotes}" />
                    <TextBlock
                        Height="23"
                        Margin="10,0,0,0"
                        Text="{Binding Data.NumberOfValidVotes}" />
                </StackPanel>

                <StackPanel HorizontalAlignment="Center" Orientation="Horizontal">
                    <TextBlock Height="23" Text="{x:Static l:Language.NumberOfInvalidVotes}" />
                    <TextBlock
                        Height="23"
                        Margin="10,0,0,0"
                        Text="{Binding Data.NumberOfInvalidVotes}" />
                </StackPanel>

                <StackPanel HorizontalAlignment="Center" Orientation="Horizontal">
                    <TextBlock Height="23" Text="{x:Static l:Language.NumberOfVotesWithoutRight}" />
                    <TextBlock
                        Height="23"
                        Margin="10,0,0,0"
                        Text="{Binding Data.NumberOfVotesWithoutRight}" />
                </StackPanel>
            </StackPanel>
        </Grid>
        <StackPanel HorizontalAlignment="Center" Orientation="Horizontal">

            <Button
                Width="80"
                Height="23"
                Command="{Binding ShowGraphCmd}">
                Show graph
            </Button>
            <Button
                Width="80"
                Height="23"
                Command="{Binding ExportToCsvCmd}">
                ExportToCsv
            </Button>
            <Button
                Width="80"
                Height="23"
                Command="{Binding ExportToPdfCmd}">
                ExportToPdf
            </Button>
            <Button
                Width="80"
                Height="23"
                Command="{Binding LogoutCmd}">
                Logout
            </Button>
        </StackPanel>
    </StackPanel>
</UserControl>